# AWS SAA 試験頻出パターン集

---

## **データベース選択の決定木**
```
【質問: どのデータベースを使うべきか？】

├─ リレーショナルDB（構造化データ）が必要
│   ├─ MySQL/PostgreSQL互換が必要
│   │   ├─ サーバーレスで自動スケール → Aurora Serverless
│   │   ├─ 最高の可用性（35秒以内フェイルオーバー） → Aurora マルチAZ DBクラスター
│   │   ├─ 読み取り負荷が高い → RDS + リードレプリカ
│   │   └─ 接続数が多い（Lambda大量実行等） → RDS Proxy
│   └─ SQL Server/Oracle → RDS
│
├─ NoSQL（非構造化データ）が必要
│   ├─ キー・バリューストア、ミリ秒レイテンシ → DynamoDB
│   │   ├─ トラフィック予測可能 → プロビジョンドモード
│   │   └─ トラフィック予測不可能 → オンデマンドモード
│   └─ ドキュメントDB（MongoDB互換） → DocumentDB
│
├─ インメモリキャッシュが必要
│   ├─ Redis機能（ソート済みセット、Pub/Sub等） → ElastiCache for Redis
│   └─ シンプルなキャッシュ → ElastiCache for Memcached
│
└─ 検索・分析が必要
    ├─ フルテキスト検索、ログ分析 → OpenSearch Service
    └─ データウェアハウス → Redshift
```

---

## **ストリーミング・メッセージング選択の決定木**
```
【質問: どのサービスでデータを連携すべきか？】

├─ リアルタイムストリーミングデータ処理
│   ├─ カスタム処理が必要 → Kinesis Data Streams
│   │   ├─ 複数コンシューマーで高スループット → 拡張ファンアウト
│   │   └─ 通常の処理 → 標準コンシューマー
│   ├─ S3/Redshift/OpenSearchへ自動配信 → Data Firehose
│   └─ Kafka互換が必要 → Amazon MSK
│
├─ 非同期メッセージキュー
│   ├─ シンプルなキュー → SQS
│   │   ├─ コスト削減重視 → ロングポーリング有効化
│   │   ├─ 重複処理防止 → 可視性タイムアウト調整
│   │   └─ 遅延配信が必要 → 遅延キュー（最大15分）
│   └─ AMQP/MQTT等のプロトコル必要 → Amazon MQ
│
├─ Pub/Sub（1対多配信）
│   ├─ AWSサービス間の簡易連携 → SNS
│   └─ 複雑なルーティング、外部連携 → EventBridge
│
└─ ファンアウトパターン
    └─ SNS → 複数SQSキュー
```

---

## **ストレージ選択の決定木**
```
【質問: どのストレージを使うべきか？】

├─ オブジェクトストレージ
│   ├─ 大容量データ、静的コンテンツ → S3
│   │   ├─ 災害対策が必要 → S3 CRR（クロスリージョンレプリケーション）
│   │   ├─ 高速転送が必要 → Transfer Acceleration
│   │   ├─ バージョン管理 → バージョニング有効化
│   │   └─ CloudFront経由のみアクセス → OAC設定
│
├─ ブロックストレージ
│   ├─ EC2にアタッチ（永続化） → EBS
│   │   └─ 停止せずにサイズ変更 → Elastic Volumes
│   └─ 超高速I/O（揮発性OK） → インスタンスストア
│
├─ ファイルストレージ
│   ├─ Linux（NFS） → EFS
│   │   └─ 高スループット保証 → プロビジョンドスループットモード
│   ├─ Windows（SMB） → FSx for Windows File Server
│   ├─ NFS + SMB両対応 → FSx for NetApp ONTAP
│   └─ HPC/機械学習（超高速） → FSx for Lustre
│
└─ ハイブリッドストレージ
    ├─ オンプレ + AWS連携 → Storage Gateway
    │   ├─ 頻繁アクセスのみローカル → キャッシュ型
    │   └─ 全データローカル → 保管型
    └─ 大容量データ物理転送 → Snowball Edge
```

---

## **ネットワーク接続パターン**
```
【質問: どうネットワークを構成すべきか？】

├─ VPC間接続
│   ├─ 2つのVPC → VPCピアリング
│   ├─ 3つ以上のVPC → Transit Gateway
│   └─ アプリケーション層サービスメッシュ → VPC Lattice
│
├─ AWSサービスへのプライベート接続
│   ├─ S3/DynamoDB → ゲートウェイ型VPCエンドポイント
│   └─ その他サービス → インターフェイス型（PrivateLink）
│
├─ オンプレミス接続
│   ├─ 専用線（高速・安定） → Direct Connect
│   │   └─ 複数ユーザーで共有 → ホスト接続
│   └─ VPN（インターネット経由） → Site-to-Site VPN
│
├─ グローバルアクセス最適化
│   ├─ 静的コンテンツ配信 → CloudFront
│   └─ 動的コンテンツ、複数リージョン負荷分散 → Global Accelerator
│
└─ IPv6専用アウトバウンド → Egress-Only IGW
```

---

## **移行戦略パターン**
```
【質問: どう移行すべきか？】

├─ サーバー移行
│   ├─ リフト＆シフト（そのまま移行） → Application Migration Service
│   ├─ コンテナ化 → App2Container
│   └─ 事前調査 → Application Discovery Service
│
├─ データ移行
│   ├─ オンライン転送（ネットワーク経由）
│   │   ├─ 継続的同期 → DataSync
│   │   ├─ SFTP/FTP経由 → Transfer Family
│   │   └─ SaaSアプリ連携 → AppFlow
│   └─ オフライン転送（物理デバイス）
│       ├─ エッジ処理も必要 → Snowball Edge Compute Optimized
│       └─ 大容量転送のみ → Snowball Edge Storage Optimized
│
└─ データベース移行 → DMS（Database Migration Service）
```

---

## **スケーリング戦略パターン**
```
【質問: どうスケールさせるべきか？】

├─ コンピューティング
│   ├─ EC2 Auto Scaling
│   │   ├─ メトリクスベース → CloudWatchメトリクス
│   │   └─ SQSキューベース → バックログメトリクス
│   ├─ ECS/EKS
│   │   ├─ ポッド単位 → HPA（Horizontal Pod Autoscaler）
│   │   └─ ノード単位 → Karpenter
│   └─ サーバーレス
│       ├─ Lambda → 自動スケール（同時実行数制限のみ設定）
│       └─ Fargate → タスク数指定
│
└─ データベース
    ├─ RDS → リードレプリカ追加（読み取り負荷分散）
    ├─ Aurora → Auto Scaling（リーダー自動追加）
    └─ DynamoDB → Auto Scaling or オンデマンドモード
```

---

## **災害対策（DR）戦略**
```
【RTO/RPOによる選択】

├─ Backup and Restore（バックアップ・復元）
│   RTO: 数時間〜数日 / RPO: 数時間
│   ├─ 実装: AWS Backup、S3スナップショット
│   └─ コスト: 最安
│
├─ Pilot Light（パイロットライト）
│   RTO: 数十分〜数時間 / RPO: 数分
│   ├─ 実装: 最小限のリソースを常時起動、障害時にスケールアップ
│   └─ コスト: 低
│
├─ Warm Standby（ウォームスタンバイ）
│   RTO: 数分 / RPO: 秒〜数分
│   ├─ 実装: 縮小版システムを常時稼働、Route 53フェイルオーバー
│   └─ コスト: 中
│
└─ Multi-Site Active/Active（マルチサイト・アクティブ）
    RTO: ほぼゼロ / RPO: ほぼゼロ
    ├─ 実装: 複数リージョンで同時稼働、Global Accelerator
    └─ コスト: 最高
```

---

## **セキュリティベストプラクティス**
```
【セキュリティ要件ごとの実装】

├─ データ暗号化
│   ├─ 保管中（At Rest）
│   │   ├─ S3 → SSE-S3 / SSE-KMS / SSE-C
│   │   ├─ EBS → KMS暗号化
│   │   └─ RDS → 透過的データ暗号化（TDE）
│   └─ 転送中（In Transit）
│       ├─ HTTPS/TLS強制
│       └─ VPN/Direct Connect暗号化
│
├─ アクセス制御
│   ├─ IAM → ユーザー/ロール/ポリシー
│   ├─ S3 → バケットポリシー + ACL
│   └─ VPC → セキュリティグループ + NACL
│
├─ 認証・認可
│   ├─ 企業SSO → SAML 2.0対応IdP
│   ├─ ソーシャルログイン → OIDC対応IdP
│   └─ API認証 → Cognito User Pools
│
└─ セキュリティ監視
    ├─ 脅威検出 → GuardDuty
    ├─ 構成監査 → Config
    ├─ API監査 → CloudTrail
    └─ 一元管理 → Security Hub
```

---

## **コスト最適化パターン**
```
【コスト削減の判断基準】

├─ コンピューティング
│   ├─ 常時稼働ワークロード → リザーブドインスタンス（1年/3年契約）
│   ├─ 柔軟なワークロード → Savings Plans
│   ├─ 中断可能なワークロード → スポットインスタンス
│   └─ サーバーレス化可能 → Lambda / Fargate
│
├─ ストレージ
│   ├─ S3ストレージクラス最適化
│   │   ├─ 頻繁アクセス → S3 Standard
│   │   ├─ 月1回程度 → S3 Standard-IA
│   │   ├─ アーカイブ（即座取得） → S3 Glacier Instant Retrieval
│   │   └─ 長期アーカイブ → S3 Glacier Deep Archive
│   └─ ライフサイクルポリシー → 自動移行
│
├─ データベース
│   ├─ 予測可能な負荷 → プロビジョンドキャパシティ + リザーブド
│   ├─ 変動が大きい → Aurora Serverless / DynamoDB オンデマンド
│   └─ 開発環境 → 定時停止・起動の自動化
│
└─ ネットワーク
    ├─ データ転送料削減 → CloudFront（キャッシュ）
    ├─ NAT Gateway削減 → VPCエンドポイント使用
    └─ リージョン間転送削減 → 同一リージョン設計
```

---

## **試験での「引っかけ」パターン**

### **パターン1: 過剰なソリューション**
```
❌ 間違い: 「高可用性のため、必ずマルチリージョン構成」
✅ 正解: マルチAZ構成で十分なケースが多い（コスト効率）
```

### **パターン2: 運用負荷の見落とし**
```
❌ 間違い: 「EC2にMySQLをインストール」
✅ 正解: RDS使用で運用負荷削減（問題文で「運用の手間を最小限」がヒント）
```

### **パターン3: スケーラビリティの見落とし**
```
❌ 間違い: 「単一のNATゲートウェイで全トラフィック処理」
✅ 正解: 各AZにNATゲートウェイ配置（単一障害点解消）
```

### **パターン4: セキュリティの見落とし**
```
❌ 間違い: 「パブリックサブネットにRDS配置」
✅ 正解: プライベートサブネットにRDS配置（最小権限の原則）
```

### **パターン5: コストの見落とし**
```
❌ 間違い: 「全データをS3 Standard保存」
✅ 正解: ライフサイクルポリシーで自動的にGlacierへ移行
```

---

## **試験直前チェックリスト**

### **必ず覚えるべき数値**
- **S3**: 11 9's耐久性、無制限容量、単一オブジェクト最大5TB
- **EBS**: 最大64,000 IOPS（io2 Block Express）
- **Lambda**: 最大15分実行、メモリ最大10GB
- **SQS可視性タイムアウト**: デフォルト30秒、最大12時間
- **SQS遅延キュー**: 最大15分
- **SQSロングポーリング**: 最大20秒
- **DynamoDB PITR**: 35日間
- **RDS自動バックアップ**: 最大35日保持
- **Aurora**: 6コピー（3AZ × 2）、4/6書き込み確認、3/6読み取り可能

### **頻出キーワードと対応サービス**
- 「運用の手間を最小限」→ フルマネージドサービス（RDS、Aurora、DynamoDB等）
- 「コスト最適化」→ リザーブド、Savings Plans、S3ライフサイクル
- 「高可用性」→ マルチAZ、Auto Scaling、ロードバランサー
- 「低レイテンシ」→ CloudFront、ElastiCache、Global Accelerator
- 「リアルタイム処理」→ Kinesis、Lambda、EventBridge
- 「疎結合」→ SQS、SNS、EventBridge
- 「セキュア」→ VPC、PrivateLink、KMS暗号化

---
