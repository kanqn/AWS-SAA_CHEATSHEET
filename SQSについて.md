# SQS (Simple Queue Service) の具体例

## **SQSの基本イメージ**

### 飲食店の「注文伝票ボックス」で例えると
```
【従来の同期処理】
お客さん → 直接厨房に注文 → 料理ができるまで待つ
                           ↓
                      厨房が忙しいと注文できない（エラー）

【SQSを使った非同期処理】
お客さん → 注文伝票をボックスに入れる（SQSにメッセージ送信）
             ↓
         厨房スタッフが空いた時に伝票を取り出す（メッセージ受信）
             ↓
         料理を作る（処理実行）
             ↓
         完了したら伝票を捨てる（メッセージ削除）
```

**メリット**:
- お客さんは注文後すぐ席に戻れる（送信側がブロックされない）
- 厨房は自分のペースで処理できる（受信側のスケーリング）
- 注文が溜まっても失われない（耐障害性）

---

## **実際のシステム例**

### **例1: ECサイトの注文処理**
```
ユーザー「購入ボタン」クリック
    ↓
Webサーバー「注文受付完了!」即座に返答
    ↓
【SQSキュー】に注文情報を格納
    ↓
バックエンドワーカー（Lambda/EC2）が処理:
  - 在庫確認
  - 決済処理
  - 配送手配
  - メール送信
```

**なぜSQSが必要？**
- ユーザーを待たせない（決済処理に10秒かかっても、画面は1秒で戻る）
- 注文が殺到しても、ワーカーが順番に処理
- ワーカーが落ちても、メッセージは失われない

---

### **例2: 動画変換サービス**
```
ユーザーが動画アップロード
    ↓
S3に保存 → S3イベント → SQSに「変換タスク」送信
    ↓
EC2ワーカー群が並列処理:
  - HD変換
  - 4K変換
  - サムネイル生成
```

**なぜSQSが必要？**
- 動画変換は時間がかかる（数分〜数時間）
- 複数のワーカーで並列処理して高速化
- ワーカーの台数を負荷に応じて増減（Auto Scaling）

---

## **ロングポーリングの具体例**

### **郵便受けをチェックする例**
```
【ショートポーリング（デフォルト）】
1秒ごとに郵便受けを見に行く
  → 手紙がなくても往復する（無駄な通信コスト）
  
【ロングポーリング】
郵便受けの前で最大20秒待機
  → 手紙が来たらすぐ取る
  → 来なくても20秒後に1回だけ戻る（コスト削減）
```

### **システム例: チャットアプリのメッセージ受信**
```python
# ショートポーリング（非推奨）
while True:
    messages = sqs.receive_message(QueueUrl=queue_url)
    if not messages:
        time.sleep(1)  # 無駄な待機
    # リクエスト数が多い = コスト増

# ロングポーリング（推奨）
messages = sqs.receive_message(
    QueueUrl=queue_url,
    WaitTimeSeconds=20  # 最大20秒待つ
)
# メッセージが来たら即座に返る
# 来なくても20秒で1回だけリクエスト
```

**効果**:
- API呼び出し回数が激減（コスト削減）
- メッセージ取得の遅延が減少

---

## **可視性タイムアウトの具体例**

### **レストランの注文伝票で例えると**
```
【問題: 可視性タイムアウトがないと】
スタッフA「伝票を取った!」
スタッフB「同じ伝票が見える... 取った!」
  ↓
同じ料理を2回作ってしまう（重複処理）

【解決: 可視性タイムアウト】
スタッフA「伝票を取った!」
  → 伝票が30秒間「他のスタッフから見えなくなる」
  → Aが料理完了 → 伝票を捨てる
  
もしAが30秒以内に完了できなかったら...
  → 伝票が再び見えるようになる
  → スタッフBが引き継げる（障害時の復旧）
```

### **システム例: 画像処理サービス**
```
【設定】
可視性タイムアウト = 300秒（5分）

【処理フロー】
1. ワーカーAが「画像リサイズタスク」を取得
   → このメッセージは5分間、他のワーカーから見えない

2. 正常ケース:
   - 3分で処理完了 → メッセージ削除 → 終了

3. 異常ケース（ワーカーAがクラッシュ）:
   - 5分経過 → メッセージが再び見えるようになる
   - ワーカーBが同じタスクを取得 → リトライ成功
```

**Kananさん向けの技術的補足**:
```python
# メッセージ受信時
response = sqs.receive_message(
    QueueUrl=queue_url,
    VisibilityTimeout=300  # 5分間隠す
)

# 処理に時間がかかりそうなら延長
sqs.change_message_visibility(
    QueueUrl=queue_url,
    ReceiptHandle=receipt_handle,
    VisibilityTimeout=600  # さらに10分延長
)

# 処理完了したら削除
sqs.delete_message(
    QueueUrl=queue_url,
    ReceiptHandle=receipt_handle
)
```

---

## **遅延キューの具体例**

### **予約配信システム**
```
【シナリオ】
ユーザーが「30分後にリマインダーメール送信」を設定

【従来の方法（非効率）】
Lambda関数が1分ごとに全予約をチェック
  → 「この予約は送信時刻？」を毎回確認
  → 無駄な処理が多い

【遅延キュー使用】
SQSメッセージを「30分後に配信」設定で送信
  ↓
30分間、キュー内で待機（自動）
  ↓
30分後、自動的にワーカーに配信
  ↓
メール送信
```

### **システム例: ゲームのアイテム付与**
```
【ゲーム内イベント】
「ログインボーナスを24時間後に付与」

【実装】
user_id = "12345"
message = {"user_id": user_id, "item": "gold_100"}

sqs.send_message(
    QueueUrl=queue_url,
    MessageBody=json.dumps(message),
    DelaySeconds=86400  # 24時間後（最大15分）
)

# 注意: 実際は最大15分なので、
# 24時間の場合はStep Functionsと組み合わせる
```

### **実用例: リトライ処理の指数バックオフ**
```
【エラー時の段階的リトライ】
1回目失敗 → 5秒後にリトライ（DelaySeconds=5）
2回目失敗 → 10秒後にリトライ（DelaySeconds=10）
3回目失敗 → 30秒後にリトライ（DelaySeconds=30）

外部APIがダウン中でも、徐々に間隔を空けて負荷を減らす
```

---

## **全機能を組み合わせた実例**

### **Kananさんの経験に近い例: Discord Bot + YouTube処理**
```
【シナリオ】
Discord Botで「このYouTube動画を文字起こしして」と依頼

【アーキテクチャ】
Discord Bot
  ↓
SQSに送信（遅延キュー: 0秒）
  ↓
Lambda（ロングポーリング: 20秒）がメッセージ取得
  ↓
可視性タイムアウト: 900秒（15分）
  ↓
処理:
  1. YouTube動画ダウンロード（2分）
  2. Whisper API文字起こし（10分）
  3. Discord返信（1秒）
  ↓
メッセージ削除（完了）

【もし処理中にLambdaがタイムアウトしたら】
→ 15分後に別のLambdaが自動的にリトライ
```

---

## **試験で問われるポイント**

### **ロングポーリング**
- キーワード: 「コスト削減」「無駄なAPI呼び出しを減らす」
- 設定: `WaitTimeSeconds=20`（最大20秒）

### **可視性タイムアウト**
- キーワード: 「重複処理を防ぐ」「障害時の自動リトライ」
- 処理時間より長めに設定（例: 処理5分 → タイムアウト10分）

### **遅延キュー**
- キーワード: 「指定時間後に処理」「段階的リトライ」
- 制限: 最大15分（それ以上はStep Functions使用）

### **よくある試験問題パターン**
```
Q: 処理に平均5分かかるタスクで、稀に10分かかる場合がある。
   適切な可視性タイムアウトは？

A: 600秒（10分）または900秒（15分）
   → 最長処理時間より長く設定
```
