## **コンテナ・オーケストレーション関連**

### **ポッド (Pod)**
Kubernetesの最小デプロイ単位。1つ以上のコンテナをまとめたもの

### **AWS Fargate**
サーバーレスなコンテナ実行環境。EC2を管理せずにコンテナを動かせる（ECS/EKS両対応）

### **Amazon ECS Anywhere**
AWS外部のオンプレミスサーバー/VMをECSクラスターに登録し、コンテナを実行・管理する機能

### **マネージドノードグループ**
EKS（Kubernetes）のワーカーノード（EC2）をAWSが自動管理してくれる機能

### **Karpenter**
EKSのオートスケーリングツール。ポッドの要求に応じてノードを動的に追加・削除

### **Horizontal Pod Autoscaler (HPA)**
Kubernetesの機能。CPU使用率などに応じてポッド数を自動増減

### **Load Balancer Controller**
EKS/Kubernetesで、ALB/NLBを自動作成・管理するコントローラー

#### **ALB (Application Load Balancer)**
- レイヤー7（HTTP/HTTPS）のロードバランサー
- パスベース/ホストベースルーティング対応
- 用途: Webアプリ、マイクロサービス、コンテナ

#### **NLB (Network Load Balancer)**
- レイヤー4（TCP/UDP）のロードバランサー
- 超高性能・低レイテンシ・固定IP対応
- 用途: ゲームサーバー、IoT、金融システム

#### **スティッキーセッション**
ALBの機能。同じユーザーのリクエストを同じインスタンスに転送してセッション維持。負荷が偏るデメリットあり

---

## **データ処理・分析関連**

### **Amazon EMRクラスター**
ビッグデータ処理フレームワーク（Hadoop、Spark等）のマネージドサービス

### **AWS Glue**
ETL（Extract:抽出、Transform:変換、Load:書き出し）サービス。複数データソースから統合・変換してターゲットへ格納

### **AWS Glueクローラー**
データソース（S3、RDS等）を自動スキャンして、メタデータ（テーブル定義）を作成

### **Amazon OpenSearch Service**
検索・ログ分析エンジン（旧Elasticsearch Service）。ログ可視化やフルテキスト検索に使用

### **サブスクリプションフィルター**
CloudWatch Logsの機能。Data FirehoseやLambda、OpenSearch Serviceなどへログデータを転送

### **Amazon Kinesis Data Streams**
リアルタイムストリーミングデータを収集・処理するサービス。データは24時間〜365日保持可能

### **Amazon Data Firehose（旧Kinesis Data Firehose）**
ストリーミングデータをS3/Redshift/OpenSearchなどへ**自動変換・配信**するフルマネージドサービス

#### **Kinesis Data Streams vs Data Firehose**
- **Data Streams**: リアルタイム処理、カスタムコンシューマー必要、データ保持期間指定可能
- **Data Firehose**: 自動配信、変換機能あり、リアルタイム性は低い、運用が簡単

### **コンシューマ**
Kinesis Data Streamsからデータを読み取って処理するアプリケーション（Lambda、EC2等）

### **拡張ファンアウト**
Kinesis Data Streamsで、各コンシューマーが専用のデータスループットを確保できる仕組み（スループット2MB/秒/シャード）

### **Amazon MSK (Managed Streaming for Apache Kafka)**
Apache Kafkaのフルマネージドサービス。大量ストリーミングデータをリアルタイム処理

---

## **データベース関連**

### **Aurora Serverless**
DBインスタンスの負荷に応じて、自動的に起動・停止・スケールアップ/ダウンを実施する機能

### **Amazon DocumentDB**
フルマネージド型のドキュメントデータベース。MongoDB互換で既存アプリを流用可能

### **RDS for PostgreSQLのpgvector拡張**
ベクトルデータ型を扱えるようにする拡張機能。SQLで類似度検索が可能（生成AI向け）

### **RDSリードレプリカ**
メイン（プライマリ）DBから非同期でデータをコピーするセカンダリDB。読み取り負荷分散に使用

### **RDSマルチAZ DBクラスター**
3つのAZに配置（1ライター + 2リーダー）。フェイルオーバー時間35秒未満で高可用性

### **Amazon RDS Proxy**
RDS/Auroraへの接続を効率管理するプロキシサービス。接続プーリングで接続数を最適化

### **DynamoDB プロビジョンドモード**
読み書きキャパシティユニット（RCU/WCU）を事前に指定。コスト予測可能、スループット保証

### **DynamoDB オンデマンドモード**
使用量に応じて自動スケール。キャパシティ管理不要、トラフィック予測不要な場合に最適

### **キャパシティユニット**
DynamoDBの読み書き性能単位。1 RCU = 4KB/秒の強い整合性読み取り、1 WCU = 1KB/秒の書き込み

### **DynamoDB Streams**
テーブルの変更（追加・更新・削除）を24時間保持するログ。Lambdaトリガーでリアルタイム処理可能

### **ポイントインタイムリカバリ (PITR)**
過去35日間の任意の時点に秒単位でデータを復元できる差分バックアップ機能（Aurora、DynamoDB等）

### **ElastiCache - ライトスルー戦略**
DB書き込み時に同時にキャッシュにも書き込む。データ整合性高いが、書き込み遅延増加

### **ElastiCache - 遅延読み込み戦略（Lazy Loading）**
キャッシュミス時のみDBから取得してキャッシュ保存。キャッシュ効率的だが初回アクセス遅い

### **TTL (Time To Live)**
キャッシュやストレージでデータが保持される時間。期限切れで自動削除

---

## **ストレージ関連**

### **S3バージョニング機能**
S3オブジェクトの過去バージョンを保持。誤削除や上書きからデータを保護

### **S3クロスリージョンレプリケーション (CRR)**
異なるリージョンのS3バケット間でオブジェクトを自動複製。災害対策・低レイテンシアクセスに使用

### **S3 Transfer Acceleration**
ユーザーとS3バケット間のデータ転送を高速化（CloudFrontエッジロケーション経由）

### **S3 Storage Lens**
S3バケットのストレージ使用状況やアクティビティメトリクスを可視化・分析する機能

### **S3 OAC (Origin Access Control)**
S3コンテンツへのアクセスをCloudFront経由に限定。直接アクセスを防ぐセキュリティ機能

### **Amazon FSx for Lustre**
機械学習やHPC（High Performance Computing）向けの超高速ストレージサービス

### **Amazon FSx for NetApp ONTAP**
NFS/SMB両対応のマルチプロトコル共有ファイルストレージ。同じデータに同時アクセス可能

#### **NetApp SnapMirror**
FSx for NetApp ONTAPの標準機能。差分同期方式で継続的・自動的なデータレプリケーション

### **Elastic Volumes**
稼働中のEBSボリュームのサイズ拡張・タイプ変更・IOPS調整をインスタンス停止なしで実行

### **EC2インスタンスストア**
EC2インスタンスの揮発性ブロックストレージ。インスタンス停止でデータ消失、超高速I/O

### **EFSプロビジョンドスループットモード**
EFSのスループットを事前指定して確保（バースト性能に依存しない）

### **AWS Storage Gateway**
オンプレミスからAWSストレージへのアクセスを高速・セキュアに行うサービス

#### **ボリュームゲートウェイ - キャッシュ型**
頻繁アクセスデータのみローカルキャッシュ、低頻度データはS3保存。階層化ストレージ

#### **ボリュームゲートウェイ - 保管型**
全データをローカル保持で高速アクセス。S3へスナップショットでバックアップ

---

## **データ移行・同期関連**

### **AWS DataSync**
オンプレミスとAWS間、またはAWSサービス間でデータを高速・自動転送するデータ移行サービス

### **AWS Transfer Family**
SFTP/FTPS/FTP経由でS3/EFSへファイル転送できるマネージドサービス

#### **Transfer Familyマネージドワークフロー**
ファイルアップロード/ダウンロードをトリガーに処理を自動実行する機能

### **Amazon AppFlow**
SaaSアプリ（Salesforce、Slack等）とAWSサービス（S3、Redshift等）間でデータを安全に転送

### **AWS Application Discovery Service**
オンプレミスのサーバー/DBを自動検出し、AWS移行を支援するサービス

---

## **アプリケーション移行関連**

### **AWS Application Migration Service**
オンプレミスサーバーをAWSへリフト＾シフト移行（物理/仮想サーバー丸ごと移行）

### **AWS App2Container**
既存のJava/.NETアプリをコンテナ化して、ECS/EKSに移行するツール

---

## **アプリケーションデプロイ・ワークフロー関連**

### **AWS Elastic Beanstalk**
アプリケーションコードをアップロードするだけで、インフラ（EC2、LB等）を自動構築・管理するPaaS

### **AWS Step Functions**
複数のAWSサービスをワークフローとして連携させ、処理を自動化するサーバーレスサービス

#### **標準ワークフロー**
- 最長1年間の実行可能
- 用途: 長期バッチ処理、業務プロセス
- 課金: 状態遷移数

#### **Expressワークフロー**
- 最大5分の実行制限
- 用途: 大量リクエストの高速処理
- 課金: 実行時間と回数

### **AWS Service Catalog**
組織承認済みサービス/アプリをポートフォリオとして作成・管理し、組織内で共有できるサービス

### **拡張ネットワーキング**
拡張ネットワーキングは、Amazon EC2インスタンスのネットワーク性能を向上させる機能です。  
インスタンス間での高スループット・低レイテンシーを実現できるため、リアルタイム性が求められるアプリケーションに最適です。  
さらに、追加料金もかかりません。  
  
---

## **ネットワーク関連**

### **VPC Lattice**
異なるVPC/アカウント間でサービス接続を簡素化するサービスメッシュ。HTTP/HTTPS/gRPC対応

### **VPCフローログ**
VPCフローログは、VPC内のENIに流れるネットワークトラフィック情報を出力する機能  
  

### **サービスディスカバリー機能**
他サービスの位置情報（IPアドレス・ポート番号）を動的に検出（VPC Lattice、ECS等で使用）

### **Transit Gateway**
複数のVPC/オンプレミスネットワークを中央ハブで接続するルーター

### **AWS Direct Connectのホスト接続**
Direct Connectパートナーの物理接続を複数AWSユーザーで共有するオプション

### **AWS PrivateLink (インターフェイス型VPCエンドポイント)**
VPC外AWSサービス（S3、CloudWatch等）へプライベートIPで接続。ENI作成してリンク

### **Elastic IP**
AWSの静的IPアドレス。インスタンスに永続的に割り当て可能

### **Egress-Onlyインターネットゲートウェイ**
IPv6専用のインターネットゲートウェイ。アウトバウンドのみ許可（NATゲートウェイ + IGWの特性）

### **AWS Global Accelerator**
ユーザーからAWSリソースへのアクセス経路をAWSグローバルネットワークで最適化。複数リージョン負荷分散可能

### **Amazon CloudWatch Network Monitor**
AWS内外とのネットワーク接続状態を継続的に監視する機能

### **Elastic Fabric Adapter (EFA)**
高性能ネットワークインターフェース。HPC/機械学習向けで低レイテンシ・高スループット（特定EC2タイプのみ）

---

## **Route 53 ルーティングポリシー**

### **レイテンシールーティングポリシー**
ユーザーに最も低レイテンシなリージョンへルーティング

### **加重ルーティングポリシー**
指定した重み付けに応じてトラフィックを分散（A/Bテスト、段階的移行に使用）

### **フェイルオーバールーティングポリシー**
プライマリリソース障害時、自動的にセカンダリリソースへ切り替え

---

## **メッセージング関連**

### **Amazon SQS (Simple Queue Service)**
フルマネージド型メッセージキューサービス。非同期処理の疎結合を実現

#### **ロングポーリング**
SQSキューが空の場合、最大20秒待機してメッセージ取得。API呼び出し削減でコスト削減

#### **可視性タイムアウト**
メッセージ受信後、指定時間（デフォルト30秒）他のクライアントから見えなくする。重複処理防止

#### **遅延キュー**
メッセージをキュー追加後、指定時間経過後に配信可能にする機能（最大15分）

#### **SQSキューのバックログ**
キューに溜まっている未処理メッセージ数。Auto Scalingのメトリクスに使用

### **Amazon SNS (Simple Notification Service)**
Pub/Subモデルのメッセージング。1つのトピックから複数のサブスクライバーへ配信

#### **SNSトピックとSQSキューの連携**
SNS → 複数SQSキュー配信で、ファンアウトパターン実現（1メッセージを複数サービスで処理）

### **Apache ActiveMQ / Amazon MQ**
AMQP（Advanced Message Queuing Protocol）対応のメッセージブローカー。既存アプリ移行に最適

### **Amazon EventBridge**
イベント駆動アーキテクチャのイベントバス。AWSサービス間や外部SaaSとイベント連携

#### **EventBridge API destinations**
外部APIへHTTPSリクエストを非同期送信できる機能。イベント発生時に外部エンドポイント連携

---

## **運用自動化・管理関連**

### **AWS Systems Manager Session Manager**
EC2インスタンスへブラウザやCLIからセキュアにログイン。SSH穴あけ不要、キーペア管理不要

### **AWS Systems Manager Automation**
運用タスク（パッチ適用、AMI作成等）を自動化。ランブック（ワークフロー定義）で実行

### **ランブック**
Systems Manager Automationで自動実行されるワークフローを定義するドキュメント

### **AWS Systems Manager Patch Manager**
EC2/オンプレミスサーバーのOSパッチ情報をスキャンし、パッチ適用を自動化

### **AWS Systems Manager Run Command**
EC2/オンプレミスサーバーに対してリモートでコマンド/スクリプトを安全に実行

### **AWS Systems Manager Parameter Store**
サービスの設定値やシークレットを安全に保存するサービス（無料枠あり）

---

## **監視・ログ・トレーシング関連**

### **AWS CloudTrail**
AWSアカウント内のAPI呼び出しを記録・監視するサービス（ガバナンス・コンプライアンス用）

### **Amazon CloudWatch Logs**
ログデータを収集・監視・保存するサービス（サブスクリプションフィルターで転送可能）

### **AWS X-Ray**
アプリケーションの動作データを収集・分析してパフォーマンス問題を特定するサービス（分散トレーシング）

---

## **セキュリティ関連**

### **Amazon Security Hub**
各種セキュリティサービス（サードパーティ含む）のアラート/データを自動収集・集約し一元管理

### **OpenID Connect (OIDC) 対応IdP**
OAuth 2.0ベースの認証プロトコル。GoogleやFacebookなどのIDプロバイダー連携

### **SAML 2.0対応IdP**
企業のActive DirectoryなどとSSO連携するための認証標準

---

## **コスト管理・最適化関連**

### **AWS Trusted Advisor**
コスト最適化、パフォーマンス、セキュリティ等6カテゴリで自動診断・推奨事項を提供

### **AWS Cost Explorer**
AWSリソースの使用量・コストをグラフで視覚化し、詳細分析できるサービス

### **AWS Compute Optimizer**
コンピューティングリソース（EC2、Lambda等）の設定を分析し、コスト最適化の推奨事項を提供

### **コスト配分タグ**
AWSリソースにキー・値のタグを付けてコスト管理・追跡を効率化（例: Project=ProjectA）

### **Cost and Usage Reports**
全アカウントの利用状況・コスト情報を一元集約するレポート（複数アカウント分析に最適）

---

## **バックアップ・災害対策関連**

### **AWS Backup**
EC2、EFS、RDS、DynamoDB等のAWSリソースを一元管理するフルマネージドバックアップサービス

### **AWS Elastic Disaster Recovery**
オンプレミス/他クラウドのサーバーをAWSへ継続的レプリケーション。災害時に迅速復旧（DR）

### **S3アクティブ/パッシブ構成**
全リクエストをプライマリリージョンに送信。障害時のみセカンダリへフェイルオーバー

### **S3アクティブ/アクティブ構成**
複数リージョンで同時稼働。S3 CRRでデータ同期し、どのリージョンでも最新データアクセス可能

---

## **ハイブリッド・エッジ関連**

### **AWS Outposts**
AWSインフラ/サービスをオンプレミスに拡張して利用できるフルマネージドサービス

### **AWS Snowball Edge Compute Optimized**
エッジコンピューティング+データ転送デバイス。52 vCPU、EC2/Lambda実行可能

### **AWS Snowball Edge Storage Optimized**
大容量データ転送特化デバイス。80TB usableストレージ、ネットワーク帯域不要

---

## **リソース共有・配置関連**

### **AWS Resource Access Manager**
VPC、サブネット、Transit Gatewayなどのリソースを他のAWSアカウントと共有する機能

### **プレイスメントグループ**
複数EC2インスタンスを論理的にグループ化。低レイテンシ通信やハードウェア障害影響軽減

---

## **AI・機械学習関連**

### **Amazon Rekognition (Video)**
動画/画像内の物体・人物・アクティビティ・不適切コンテンツを自動検出するAIサービス

### **Amazon Polly**
機械学習を利用したテキスト読み上げ（Text-to-Speech）サービス

## **ゲートウェイ型 インターフェイス型の違い**
  
インタフェース型エンドポイント  
AWSサービスのエンドポイントへのトラフィックを、インターネットを経由せずに、AWSのプライベートネットワーク経由で直接ルーティングできるようにします  

ゲートウェイ型エンドポイント  
通常はインターネット経由でアクセスされるAWSサービス（例: S3, DynamoDB）に、VPCからプライベートにアクセスできるようにする機能  
  
---

## **リトライ戦略**

### **エクスポネンシャルバックオフ**
API失敗時、リトライ間隔を指数関数的に増加させる戦略（例: 1秒→2秒→4秒→8秒）。サービス過負荷防止
