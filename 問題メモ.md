```
・コンテナオーケストレーションにEKSを使用する場合、
　Kubernetesクラスタの設定と管理が必要となり管理負担が増えます

・Amazon Athenaはサーバーレスですので、
インスタンスやクラスターなどのインフラの管理が不要で、
プロトタイプの検証や手早く分析をしたいようなケースに向いています

・AWS Configでは、ACMで管理している証明書の有効期限を対象としたルールを作成できます

・問題文にMongoDBを移行するとあればDocumentDBに移行するが解になりやすい

・Amazon CloudWatch LogsからOpenSearch Serviceへ直接データをストリーミングすることが可能

・gRPC、サービスディスカバリとでたらVPC Latticeが解になりやすい

・キーの削除操作をキャンセルするイベントを設定する必要があるとあったらSystems Manager Automationのランブックが解になりやすい

・アクセスが減少しアクセスパターンが一定でなくなるログファイルの保管
　→S3 Intelligent-Tieringでアクセスパターンに基づいて
　 自動でデータを適切なコスト効率のよいストレージ階層に移動

・DynamoDBで一定時間経ったら自動でファイル削除したいという問題
　→TTL機能で削除するが解になりやすい

・コスト増加の原因を特定するグラフを作成
　→分析してグラフにしてくれるCost Explorerが解になりやすい

・APIにおいて使用量プランは、REST APIでのみ利用可能な機能

・コストを抑えてつつ低遅延で全世界向けに静的ウェブサイトを運営したい
　→コストを抑えるという意味で制限されたエッジロケーションのCloudFrontを使う

・コスパを優先で運用コストを抑えつつ、利用率に変動のあるデータベースを使いたい
　→Aurora Serverlessが解になりやすい

・EBSボリュームで、既存のブロックストレージ構成を維持したまま、申請に応じて割り当て容量を拡張したい
　→Elastic Volumes機能でEBSボリュームのサイズを拡張する

・データ転送コスト削減するにはどうすればいいか答える問題
　→同一AZ内が選択肢にあれば無料なため解になりやすい

・POSIX準拠のストレージであることという問題
　→EFSが解になりやすい

・SQSキューで処理が完了する前に、他のインスタンスが同一のメッセージを受け取っている
　→可視性タイムアウト値を増やす

・アプリケーションのRPO（目標復旧時点）は1秒未満を目標にするDBを設計したい
　→RDS MySQLデータベースのマルチAZ配置を使用する

・インターネットサービスプロバイダーが提供するDNSサービスのパフォーマンス低下に悩まされている
　→Route 53のパブリックホストゾーンを設定し、現在のプロバイダーからの全DNSレコードをインポートする。
　 DNSレコードの移行後、ドメインのネームサーバーをRoute 53に変更する

・DBの設計で、アプリケーションのダウンタイムを最小限に抑えたい
　→マルチAZ DBクラスター構成が解になりやすい

・OpenSearch Serviceは大量のデータに対して高速な検索や分析を実現する
　→収集したデータを高速なテキスト検索できるようにしたい場合はこれが解

・運用の手間を減らし、自動的にスケールアップしたい
　→Lambdaが解になりやすい

・高性能コンピューティングで、高速データ転送と低遅延を実現したい
　→EFAがあれば解になりやすい

・外部のAPIと連携する必要がある
　→API destinationsがあればそれが解になりやすい

・イベント駆動型のワークフローを構築しなさい
　→Step Functionsが解になりやすい

・製品の管理者が製品のバージョンや、製品がいつ・誰に使用されたかを一元的に管理したい
　→AWS Service Catalogの製品

・API Gatewayで、新しいバージョンのAPIをリリースしたい
　→デプロイステージにCanaryリリースを設定し、新しいAPIバージョンをステージにデプロイする

・オンプレミスや他のクラウド上で動作しているサーバーやアプリケーションをAWSに継続的にレプリケーションし、
　障害や災害発生時にシステムを迅速かつ低ダウンタイムで復旧できるようにしたい
　→AWS Elastic Disaster Recoveryが解になりやすい

・IAMユーザーおよびIAMグループ、IAMロールなどの運用を見直した。以降、IAMリソースの設定が変更された場合に検知したい
　→IAMリソースを監視するAWS Configルールを作成する

・Amazon FSx for NetApp ONTAP上に保管しているデータを運用の手間を増やさずにAWSに短いダウンタイムと迅速な切り替えで複製したい
　→NetApp SnapMirrorが解になりやすい

・DBの問題で、参照パフォーマンスに関する言及がある
　→リードレプリカが解になりやすい

・オンプレミスとAWS間の通信における遅延やパケットロスを継続的に監視し、異常を早期に検知できる仕組みを求めている
　→CloudWatch Network MonitorのNetwork Synthetic Monitorが解になりやすい

・インスタンス間の通信遅延を最小限に抑える(リアルタイム性重視)ように設計する必要がある
　→EC2クラスタープレイスメントグループの拡張ネットワーキングを有効化が解になりやすい
```

## **細かい注意点集**

```
・Amazon EFSのライフサイクル設定は、ファイルシステム全体に対して適用されるため、
　特定のファイルだけに対してライフサイクル管理を無効化したり、
　標準ストレージに固定するような設定はできません。


・EC2で動いている(長時間利用されていない期間もある)ものをコスト削減してくださいという問題があればlambdaに移行する
→EC2はインスタンスの起動時間に対して課金される一方、Lambdaは実行時間と選択したメモリの容量に対して課金される
　また、Lambdaはサーバーレスだから運用コストも削減される


・Amazon AthenaはAmazon S3上のデータに対して標準SQLを実行できるデータ分析サービスでサーバーレス
　→プロトタイプの検証や手早く分析をしたいようなケースに向いている

・リザーブドインスタンスとCompute Saving Plansの違い
　→インスタンスはEC2
　→Saving Plansは
　　FargateやLambdaといったサービスにも自動的に適用されます。
　　サービス使用量（1時間あたりの利用料金）を決めて契約すること

・lambdaとfargateでの運用コストが低いのはlambda

・ゲートウェイ型VPCエンドポイントとPrivateLinkでは、
　ゲートウェイ型の方がコスト削減になる(PrivateLinkは利用料金が発生する)

・EFSはNFSをサポートするファイルストレージサービスでEFSは複数インスタンス（複数ユーザー）からの同時アクセスを前提としている
　→全てのユーザーが最新のパッケージにアクセスできるようにするには
   各EC2インスタンスからAmazon EFSをマウントし、共有したいファイルを保存する

・FIFOには、メッセージの順序性やメッセージの重複排除機能がある

・RDSのマルチAZは可用性を向上する機能

・Auroraはデータベースへの負荷に応じて動的にレプリカインスタンスを増減するAuto Scaling機能を備えています。
　AuroraはAWS Backupにも対応していますので、細かい頻度でバックアッププランを作成したり、
　ポイントインタイムリカバリを有効化できます。

・リードレプリカは読み取り負荷の軽減とパフォーマンスの向上を主な目的とした機能

・マルチAZ配置を使用すると、プライマリDBインスタンスとスタンバイDBインスタンスを異なるAZに配置することによって
　 可用性を向上させることができます

・マルチAZ DBクラスターは、マルチAZ構成の新しい高可用性オプションで
　→3つのアベイラビリティゾーンにまたがって配置されるため、可用性がさらに強化されています
　→2つのスタンバイインスタンスがリードレプリカとしても機能し、読み取りアクセスが可能
　→フェイルオーバー時には通常35秒未満で切り替わるため、アプリケーションのダウンタイムを最小限に抑えることができます

・MemcachedはマルチAZに対応していない

・S3バケットへ転送したデータは、Amazon RDSやAmazon Auroraデータベースへインポートすることができる

・DynamoDBのグローバルテーブルは、DynamoDBテーブルを複数のリージョンにまたがって運用できるサービス
　→ユーザーは地理的に近いリージョンのDynamoDBテーブルへ高速な読み込みと書き込みが可能になる

・EMRはビッグデータの処理や分析を行うサービスで、Glueよりも大規模なデータを扱う場合に適している
　→運用にかかるコストは最小限にしたいという要件にはGlueを使うこと

・ルートテーブルをAZ間で共有すると、デフォルトルートは1つのNATゲートウェイしか登録できない

・ALBには、各ターゲットが正常に動作しているか定期的に確認するヘルスチェックの機能があり、正常なEC2インスタンスにのみ処理を振り分けることができる

・EBSとEC2インスタンスストアでは、EC2インスタンスの方が処理速度が速い

・S3はスケーラビリティに該当する

・SMBは主にWindowsコンピュータ間においてファイル共有やプリンタ共有に使用されるプロトコルですが、LinuxからもSMBクライアントを用いてマウントできます

・S3バケットから他のS3バケットへ自動的にコピーをするには「S3レプリケーション」が適切

・オンデマンドモードでは、キャパシティユニットを固定で指定することはできません。

・EC2インスタンスにMicrosoft SQL Serverをインストールすることで、SQL Serverのすべての機能をフルに活用でき、オンプレミスと同様のカスタム構成も可能

・Step Functionsを使用することで、イベント駆動型のワークフローを構築し、定義した各ステップを順次実行することが可能

・Amazon DynamoDB Accelerator（DAX）は、DynamoDB向けのインメモリキャッシュサービスで
　→DAXを利用することにより、ミリ秒（千分の一秒）だったレスポンスをマイクロ秒（百万分の一秒）レベルのパフォーマンスにまで向上させることができます。

・CloudFrontはエッジロケーションを使用して、動的コンテンツも効率的かつ高速に配信できる

・S3イベント通知は、同一条件の通知を複数の宛先に送ることはできない

・Auto Scalingのヘルスチェックのタイプには「EC2」と「ELB」があります。
　「EC2」はインスタンスのステータスチェックの結果を確認し、「ELB」は指定した接続先への応答確認をします。

・SNSとSQSを組み合わせると「ファンアウト」構成を実装できます。
　ファンアウトとは、メッセージを複数の送信先に配信し、それぞれが並列に処理を実行する仕組みのことです。
　SNSで分類されたメッセージを、後続のアプリケーションがSQSキューから取得して処理できます。

・NetApp SnapMirrorは、FSx for NetApp ONTAPに標準搭載されたデータレプリケーション機能です。
　スナップショットを利用し、変更されたデータのみを転送する差分同期方式により、
　継続的かつ自動的なレプリケーションを実現します。

・Amazon Managed Streaming for Apache Kafka (Amazon MSK) は、
　Apache KafkaをAWS上で効率的に運用できるようにするためのフルマネージドサービスです。
　大量のストリーミングデータを効率的に取り扱い、リアルタイムで処理するのに適しています。
　→これのETL形式にしたい場合はGlueを使う

・インスタンスストアは、EBSよりも高いパフォーマンスを発揮できる
```










