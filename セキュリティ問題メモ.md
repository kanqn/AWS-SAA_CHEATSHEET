```
・安全性がより高い方法でデータを転送しろという問題
　→Direct Connectを使用することが解になりやすい

・IdP側で多要素認証と統合したい
　→IAM Identify Centerと統合するが解になりやすい

・異なるリージョンのVPC間におけるセキュリティグループの設定には、
　別リージョンのVPCのセキュリティグループをインバウンドルール内で指定することはできないため、
　代わりに、VPCのCIDRブロックを指定する必要があります。

・AWS Client VPNはパソコンなどの端末とVPCのClient VPNエンドポイントをインターネットVPNで接続するサービスであるため、
　社内ネットワーク外からもアクセスできてしまいます。

・CloudFrontで使用する証明書は
　必ず米国東部バージニア北部（us-east-1）リージョンに存在している必要があります。
　また、独自ドメインを使用する場合はパブリック証明書を使用することが求められる

・S3バケット内の機密データの検出、分類をしたい
　→Amazon Macieを使うが解になりやすい

・EMRのクラスターがポリシーに準拠しているかを検出して、準拠していなければ通知
　→EventBridgeルールを設定して、Lambdaでポリシー準拠しているかを検証し、通知する

・AWS WAFをALBに割り当てて「Web ACL」で許可する国を設定することができる

・Amazon S3では直接AWS WAFを有効にできないので、
　AWS WAFのWeb ACLを適用するには静的WebサイトホスティングのWebコンテンツへのアクセスをCloudFront経由にします。

・Lambda関数からオンプレミス内のプライベートネットワーク上にあるデータベースにアクセスしたい
　→Lambda関数がVPCへアクセスできるように、VPC Lambdaの設定をする

・外部の認証局（CA）で発行された証明書は外部のCAによって管理されているため、
　ACMで自動更新することはできません。手動による更新が必要です

・安全かつ効率的にデータセットを共有しつつ、Athenaでクエリ実行したい
　→DataZoneを使用するが解になりやすい

・組織全体で共通のセキュリティグループルールを統一的に適用し、ルールの監査と未使用・冗長ルールの検出を自動化したい
　→Firewall Managerを使用するが解になりやすい

・OU階層やガードレールに関する変更を検知し、担当者に通知する仕組みを構築したい
　→Control Towerを導入してランディングゾーンを構成し、OU階層やガードレールに関する変更をドリフトとして検出し、通知を行う

・あるAWSアカウントで暗号化されたリソースを別のAWSアカウントで復号したい場合、
　KMS（Key Management Service）の暗号化鍵を複数のAWSアカウントで使用できます（クロスアカウント）

・スナップショットが暗号化されている場合は、共有先のアカウントへKMS暗号化キーの使用を許可する必要があります。

・セットアップと運用管理の手間を最小限に抑えながらSFTPを使用してデータをS3に転送したい
　→Transfer FamilyのSFTPサーバーを利用が解になりやすい

・セキュリティグループは「許可ルールのみ」を持つステートフルなファイアウォールであり、明示的な拒否ルールを定義する機能はありません

・AWS Configを有効にすることで、暗号化されていないEBSボリュームを自動的に検出し、適切なセキュリティ基準に従っていないリソースを特定することができます

・代理管理者の指定はOrganizations管理アカウントからのみ実行できる

・ゲートウェイ型VPCエンドポイントはS3とDynamoDB専用の仕組み

・IPアドレスを変更して送信される不審なリクエストが、Webサービスに対して大量に発生している
　→AWS WAFを使用し、レートベースのルールを設定する

・EKSクラスター内に格納されるシークレット（ログイン時の認証情報など）を、
　AWS KMS（Key Management Service）で管理されているキーを使用して暗号化する必要がある
　→EKSクラスター作成時に、シークレットの暗号化オプションを有効にする

・SaasでAWS間でデータを安全に転送したい
　→AppFlowでフローを設定するが解になりやすい

・CloudFrontでHTTPS通信を提供するため、このパブリックなサードパーティ認証局（CA）の証明書を利用することが必要になった
　→ローカルマシンを使用してパブリックなサードパーティ認証局（CA）から証明書を取得し、AWS Certificate Manager（ACM）にインポートする。
　　その証明書をCloudFrontディストリビューションのカスタムSSL/TLS証明書として設定する

・ALBではCookieベースのスティッキーセッションが利用可能

・オンプレミスのサーバーや他クラウド上のアプリケーションからAWSのリソースにアクセスさせたい
　→IAM Roles Anywhereを利用します

・Macieの委任管理者として指定できるのは、組織内のAWSアカウントに限られており、IAMユーザーを指定することはできません。

・オンプレミスのLDAPディレクトリサービスを利用をしてAWSマネジメントコンソールへのユーザー認証を行いたい
　→Security Token Service（STS）を使うが解になりやすい
```


### わからなかった単語

```
・Trusted AdvisorはAWSサービスの利用状況をベストプラクティスと照合し、
　改善のためのレコメンデーションを提示するサービス

・AWS ConfigはAWSリソースの設定変更を監視、記録し、
　リソースのセキュリティやコンプライアンスを評価するサービス

・AWS CloudTrailは、AWS環境内での「いつ」「誰によって（ユーザーやAWSサービス、APIなど）」
　「どのような操作が行われたのか」を記録・保存するサービス

・AWS Firewall Managerは、複数のAWSアカウントやサービスを対象に、firewallのルールを一元的に設定・管理するセキュリティ管理サービス

・AWS Lake Formationは、安全なデータレイクを簡単かつ迅速に構築し、分析に利用できるようにするためのマネージドサービス
また、Lake Formationでは、IAMの権限設定に加えて、Lake Formation独自の権限管理モデルを持っており、
これによりテーブル単位や列単位といった、きめ細かなアクセス制御を行うことができます。

・CIDRブロックとは同じネットワークプレフィックスとビット数を共有する IP アドレスの集まり

・IdPは、ユーザーの認証情報を管理し、必要に応じて
　他のサービスへその認証情報の提供を行うサービスで、SSOを可能にする

・Amazon Macieは、S3バケット内の機密データの検出・分類に特化したセキュリティサービス

・AWS Network Firewallは、VPC向けのファイアウォール機能を提供するマネージドサービス
　→IPSやドメイン名によるトラフィックのフィルタリングができる

・Cognitoのアダプティブ認証
　→新しいデバイス、位置、ネットワーク情報など、
  通常とは異なる状況でのログインを検知すると、動的にセキュリティレベルを調整する

・Route 53 Resolverは、VPCにデフォルトで提供されるDNSサーバーで、
　インターネット上に公開されたリソースの名前解決と、VPC内のリソースの名前解決ができます。

アウトバウンドエンドポイント
　→VPC内のリソースがオンプレミス内部のホスト名を解決する必要がある場合に使用

インバウンドエンドポイント
 →オンプレミスのリソースがVPC内のプライベートDNSホスト名を解決する必要がある場合に使用

・Transit VIFは、オンプレミスネットワークとAWS Transit Gateway間の
　接続を確立するためのインターフェイス
　→オンプレミスと複数のVPC間の通信がTransit Gatewayを使用して管理できるようにできる

・Amazon EMRは、Hadoopやsprakの大量のデータを高速で安価に処理、分析するサービス

・EMRランタイムロールは、クラスターで実行される各ジョブに
　異なるIAMロールを割り当てる仕組み

・タスクロール（taskRoleArn）とは、
　コンテナが他のAWSサービスを利用する際に設定するアクセス権限

・Amazon DataZoneは、AWS、オンプレミス、サードパーティーに分散されたデータを一元的にカタログ化し、
　検索・共有・管理を効率化するサービス

・AWS Control Towerは、AWSにおけるベストプラクティスに基づいた、
　セキュアなマルチアカウント環境のセットアップを自動化するマネージドサービス

Control Towerには以下の機能がある
○予防コントロール
・違反となる操作を事前にブロックします。
・実装例：OrganizationsのSCPを利用し、特定リージョン外でのEC2インスタンスの起動を禁止する。

○検出コントロール
・リソース作成後に設定を監視し、ポリシー違反を検出して通知します。
・実装例：AWS Configルールでリソースの設定変更やコンプライアンス違反を検出する。

○プロアクティブコントロール
・CloudFormationでリソース作成前に適合性をチェックし、違反リソースの作成を防止します。
・実装例：パブリックアクセス可能なS3バケットの作成を防止する。

・AWS Firewall Managerは、AWS Organizations環境で組織全体のファイアウォールのルールを一元的に設定・管理するセキュリティ管理サービス
　VPCのセキュリティグループを管理する機能があり、セキュリティグループポリシーを用いて
　OUやアカウント、リージョン単位で共通の許可ルールを定義・配布し、準拠状況を継続的に監視できます。

・STS AssumeRoleとはAWS STSのサービスの1機能のことを指していて、IAMロールに設定した権限を一時的に使えるようにTokenを発行する行為

・AWS Transfer Familyは、AWSストレージ上へのセキュアなファイル転送を実現するマネージドサービスです。
　SFTP、FTPS、FTPなどをサポートし、Amazon S3およびAmazon EFSへセキュアにファイルの転送が可能です。

・AWS IAM Access Analyzerは、AWSリソースのアクセス権限を継続的に監視・分析するサービス
　主な機能に、外部アクセス分析という組織外に対する意図しないアクセス許可設定を検出する機能がある

・WAFでは、短期間に大量のリクエストを送信してくるIPアドレスを自動的にブロックする「レート（リクエスト頻度）ベースのルール」を設定できます。
　レートベースのルールは、設定されたリクエスト頻度を超えたIPアドレスからのリクエストを自動的にブロックするので、
　IPアドレスを変更して送信される不審なリクエストにも動的にアクセス制限できます。

・AWS Secrets ManagerはAWSリソースの認証情報の管理やローテーションを行うサービス

・Amazon AppFlowは、様々なSaaSアプリケーション（例：Salesforce、ServiceNow、Slackなど）と、AWSサービス（例：Amazon S3、Redshift、DynamoDBなど）間でデータを安全に転送できるフルマネージドサービス

・Transfer Familyのマネージドワークフローは、ファイルのアップロードやダウンロードをトリガーとして、処理を自動実行できる機能です。

・DynamoDB Acceleratorで、既存のクラスターに対して後から暗号化設定を変更することはできません。
　そのため、暗号化を実装するには、既存のクラスターを削除し、必要な暗号化設定を有効にして新しいクラスターを作成する必要があります。

・Amazon Security Hubは、AWSのセキュリティサービス（GuardDuty、Inspector、Macieなど）や
　サードパーティ製ツールからアラートなどの検出情報を自動収集し、統合的に可視化・管理するサービス

・Lake Formationでのアクセス制御の方法には「名前付きリソースでのアクセスコントロール」と「タグ（LFタグ）ベースのアクセスコントロール」の2種類があります。
　タグベースの方法を使用することで、管理する権限の数を抑えることができます。

・オンプレミスのサーバーや他クラウド上のアプリケーションからAWSのリソースにアクセスさせたい場合は、
　IAM Roles Anywhereを利用します。

・S3バッチオペレーションは、S3のオブジェクトに対して大規模なバッチ処理を実行するための機能です。
　操作対象のリストに基づき、コピー、タグ付け、ACL変更、ストレージクラス変更、カスタムLambda関数の呼び出しなどが可能で、
　大量のオブジェクトに対する一括処理をサーバーレスに実行でき、運用の効率化が図れます。

・Amazon SQSでは、キューに対してアクセスポリシー（SQSポリシー）というリソースベースのポリシーを設定できます。

・Lambdaレイヤーのクォータを超えた依存パッケージの保存先は、ファイル共有ストレージであるAmazon EFSが最適

・Macieの委任管理者として指定できるのは、組織内のAWSアカウントに限られており、IAMユーザーを指定することはできません。
```
